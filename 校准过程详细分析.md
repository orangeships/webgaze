# 视线跟踪系统校准过程详细分析

## 概述

本分析详细解释了 `homtransform.py` 中视线跟踪系统的校准过程，该过程将相机坐标系中的视线向量映射到屏幕坐标系中的像素位置。整个校准系统基于刚体变换理论，使用非线性最小二乘法求解最优变换参数。

## 1. 初始化阶段

### 1.1 类初始化 (`__init__`)

```python
def __init__(self, directory, custom_width=None, custom_height=None):
    # 屏幕参数设置
    if custom_width and custom_height:
        self.width = custom_width
        self.height = custom_height
        self.width_mm, self.height_mm = self._get_monitor_physical_size(custom_width, custom_height)
    else:
        self.width, self.height, self.width_mm, self.height_mm = getScreenSize()
    
    # 校准矩阵初始化
    self.STransG = None    # 全局校准变换矩阵 (4×4)
    self.StG = None        # 局部变换矩阵列表
    self.scaleWtG = None   # 缩放因子
    self.SetVal = None     # 校准点坐标
    self.gaze = None       # 注视向量数据
    self.calibrate_pnp = None  # PnP姿态均值
```

**关键参数说明：**
- `width/height`: 屏幕像素尺寸
- `width_mm/height_mm`: 屏幕物理尺寸（毫米）
- `STransG`: 全局刚体变换矩阵，包含旋转和平移
- `StG`: 各校准点的局部平移修正向量

### 1.2 物理尺寸获取

```python
def _get_monitor_physical_size(self, target_width, target_height):
    # 通过screeninfo库获取显示器物理尺寸
    # 特殊处理：1920×1080副屏硬编码为24寸显示器尺寸
    # 返回: (width_mm, height_mm)
```

**设计考虑：**
- 自动检测显示器物理尺寸用于毫米-像素转换
- 特殊处理副屏，确保物理尺寸准确性
- 重试机制提高可靠性

## 2. 校准主流程

### 2.1 系统预热和稳定性检测

```python
def calibrate(self, model, cap, sfm=False, display_index=0, filename=None):
    # 预热阶段：30帧检测系统稳定性
    warmup_frames = 30
    required_stable_frames = 10
    
    for i in range(warmup_frames):
        ret, frame_cam = cap.read()
        if ret:
            eye_info, landmarks, pnp_info = model.get_gaze(frame=frame_cam, imshow=False)
            if eye_info is not None:
                stable_count += 1
            # 显示预热进度
            calib_ui.show_warmup_screen(i + 1, warmup_frames, stable_count, required_stable_frames)
```

**预热目的：**
- 让摄像头和算法达到稳定状态
- 确保用户适应环境
- 过滤初始不稳定的测量数据

### 2.2 Pygame界面初始化

```python
# 创建校准界面
calib_ui = PygameCalibrationUI(self.width, self.height, display_index=display_index)
calib_ui.initialize()

# 创建校准目标管理器
calib_targets = PygameCalibrationTargets(self.width, self.height)
calib_targets.start_timing()
```

**界面功能：**
- 显示校准进度和提示信息
- 管理校准目标点的显示和切换
- 处理用户交互事件

### 2.3 数据采集阶段

```python
# 校准主循环
while cap.isOpened():
    # 获取当前校准点
    idx, SetPos = calib_targets.getTargetCalibration(calib_time_per_point)
    if idx is None:
        break  # 校准完成
    
    # 显示校准点
    calib_ui.show_calibration_point(idx)
    
    # 采集注视数据
    eye_info, _, pnp_info = model.get_gaze(frame=frame_cam, imshow=False)
    
    # 记录有效数据
    if hasattr(calib_targets, 'can_record') and calib_targets.can_record:
        timestamp = time.time_ns() / 1000000000
        SetPos_mm = self._pixel2mm(SetPos)
        self.df = pd.concat([self.df, DataFrame([np.hstack((timestamp, idx, arr, SetPos_mm, 0, WTransG1.flatten()))])])
```

**数据采集特点：**
- 每个校准点停留2.2秒，确保数据质量
- 只在用户稳定注视时记录数据
- 收集完整的注视向量、头部姿态、眼睛位置等信息

### 2.4 数据结构

校准数据包含以下字段：
```python
columns = ['Timestamp', 'idx', 'gaze_x', 'gaze_y', 'gaze_z', 
          'REyePos_x', 'REyePos_y', 'LEyePos_x', 'LEyePos_y', 
          'yaw', 'pitch', 'roll', 'HeadBox_xmin', 'HeadBox_ymin',
          'RightEyeBox_xmin', 'RightEyeBox_ymin', 'LeftEyeBox_xmin', 'LeftEyeBox_ymin',
          'ROpenClose', 'LOpenClose', 'set_x', 'set_y', 'set_z'] + 16 * ['WTransG']
```

## 3. 数据预处理阶段

### 3.1 异常值去除 (`_RemoveOutliers`)

```python
def _RemoveOutliers(self):
    # 按校准点索引分组
    idx = int(pd.unique(self.df['idx'])[-1]) + 1
    
    for i in range(idx):
        # 提取gaze向量
        g_ = self.df[self.df['idx'].values == i].loc[:, 'gaze_x':'gaze_z']
        set_val = self.df[self.df['idx'].values == i].loc[:, 'set_x':'set_z']
        WTG_ = self.df[self.df['idx'].values == i].filter(like='WTransG')
        
        # 三维异常值检测
        mask = (
            self._MaskOutliers(g_.loc[:, 'gaze_x']) &
            self._MaskOutliers(g_.loc[:, 'gaze_y']) &
            self._MaskOutliers(g_.loc[:, 'gaze_z'])
        )
        
        # 应用掩码
        g[i] = g_[mask]
        s[i] = set_val[mask]
        WTG[i] = WTG_[mask]
```

**异常值检测算法：**
- 使用中位数绝对偏差(MAD)代替标准差
- 阈值设为0.8σ，提高过滤严格性
- 三维联合检测，确保数据质量

### 3.2 鲁棒性异常值检测 (`_MaskOutliers`)

```python
def _MaskOutliers(self, arr, std_threshold=0.8):
    # MAD方法（更鲁棒）
    median = np.median(arr)
    mad = np.median(np.abs(arr - median))
    
    if mad == 0:
        # 回退到标准差方法
        mean = np.mean(arr)
        std = np.std(arr)
        if std == 0:
            return np.ones_like(arr, dtype=bool)
        threshold = std_threshold * std
        mask = np.abs(arr - mean) < threshold
    else:
        # MAD方法
        threshold = std_threshold * mad * 1.4826
        mask = np.abs(arr - median) < threshold
    
    return mask
```

**算法优势：**
- 对异常值不敏感
- 自动回退机制
- 1.4826常数将MAD转换为标准差等效值

## 4. 数学模型和变换矩阵计算

### 4.1 核心数学模型

校准的核心是将**相机坐标系中的单位注视向量**转换为**屏幕坐标系中的实际位置**。这基于以下几何关系：

1. **注视射线方程**: `R(t) = t * gaze_vector` (t > 0)
2. **屏幕平面**: `Z = 0` (屏幕位于相机前方)
3. **交点计算**: 求解射线与平面的交点

### 4.2 刚体变换理论

```python
def _fitSTransG(self, gaze, SetVal, g):
    # 屏幕坐标系→相机坐标系的旋转矩阵
    SRotG = np.array([[-1, 0, 0],
                      [ 0,-1, 0], 
                      [ 0, 0, 1]])
    
    # 数据预处理
    gaze = gaze.to_numpy()[:, :, None]  # shape: (N,3,1)
    SetVal = SetVal.to_numpy()
    
    # 定义残差函数
    def alignError(x, *const):
        SRotG, gaze, SetVal = const
        StG = np.array([[x[0]], [x[1]], [x[2]]])  # 平移向量
        Gz = np.array([[0], [0], [1]])             # 屏幕法向量
        
        # 计算交点比例因子
        mu = (Gz.T @ (-SRotG.T @ StG)) / (Gz.T @ gaze)
        
        # 映射到屏幕坐标系
        Sg = SRotG @ (mu * gaze) + StG
        
        # 计算残差
        error = SetVal[:, :, None] - Sg
        return error.flatten()
```

### 4.3 几何解释

**步骤1: 坐标变换**
- `gaze`: 相机坐标系中的单位向量
- `SRotG`: 屏幕坐标系到相机坐标系的旋转矩阵
- `StG`: 屏幕平面在相机坐标系中的位置

**步骤2: 交点计算**
- `mu`: 射线参数，表示从相机到屏幕交点的距离
- `mu = (平面原点到平面的距离) / (gaze在平面法向的投影)`

**步骤3: 屏幕坐标**
- `Sg = SRotG @ (mu * gaze) + StG`: 屏幕坐标系中的交点

### 4.4 非线性优化

```python
# 初值设置
x0 = np.array([self.width / 2, self.height / 2, self.width])

# 非线性最小二乘求解
res = opt.least_squares(alignError, x0, args=const)
xopt = res.x

# 构造全局变换矩阵
StG = np.array([[xopt[0]], [xopt[1]], [xopt[2]]])
STransG = np.r_[np.c_[SRotG, StG], np.array([[0, 0, 0, 1]])]
```

**优化特点：**
- Levenberg-Marquardt算法
- 初值基于屏幕中心位置
- 返回4×4齐次变换矩阵

### 4.5 局部修正机制

```python
# 为每个校准点计算局部修正
for i in range(size):
    scaleGaze = self._getScale(np.median(g[i], axis=0), STransG)
    STransG_, GTransS_ = self._getSTransG(SRotG, self.SetValues[i],
                                         np.median(g[i], axis=0), scaleGaze)
    self.StG[i] = STransG_[:3, 3, None]  # 保存局部平移修正
```

**局部修正目的：**
- 补偿个体差异
- 提高不同注视区域的精度
- 生成个性化的变换参数

### 4.6 缩放因子计算 (`_getScale`)

```python
def _getScale(self, gaze, STransG):
    # 计算使gaze射线与屏幕相交的缩放因子
    Sg = STransG[:3,:3] @ gaze + STransG[:3,3]
    scale = -Sg[2] / gaze[2]
    return scale
```

**几何意义：**
- 确保射线与Z=0平面相交
- 补偿深度估计的不确定性
- 为局部变换提供准确的尺度信息

## 5. 系统集成和使用

### 5.1 结果保存

```python
def _save_calibration_results(self, STransG, g, SetVal, gaze, sfm, ...):
    # 保存为JSON格式，包含完整的校准信息
    results = {
        'transformation_matrix': STransG.tolist(),
        'local_corrections': [st.tolist() for st in self.StG if st is not None],
        'calibration_points': [sv.tolist() for sv in self.SetValues],
        'screen_info': {
            'width_px': self.width,
            'height_px': self.height,
            'width_mm': self.width_mm,
            'height_mm': self.height_mm
        },
        'pnp_mean': pnp_tvec_mean.tolist() if pnp_tvec_mean is not None else None
    }
```

### 5.2 精度评估

校准过程中收集的统计信息：
- 有效帧数量
- 异常值过滤比例
- 优化收敛性指标
- 各校准点的数据质量

### 5.3 实际使用

校准完成后，视线跟踪系统使用：
1. **全局变换**: `STransG` 用于基础视线映射
2. **局部修正**: 根据注视区域选择对应的 `StG[i]`
3. **缩放应用**: `_getScale` 确保几何一致性

## 6. 算法特点总结

### 6.1 优势
- **鲁棒性**: MAD异常值检测，对离群值不敏感
- **准确性**: 非线性优化+局部修正
- **自适应**: 个性化校准参数
- **完整性**: 完整的坐标变换链

### 6.2 技术亮点
- **刚体变换理论**: 基于几何约束的数学模型
- **多层次优化**: 全局+局部修正机制
- **质量控制**: 多维度数据验证
- **用户友好**: 图形化界面和实时反馈

### 6.3 应用价值
该校准系统实现了从"相机坐标系"到"屏幕坐标系"的高精度映射，为视线跟踪应用提供了可靠的几何基础。通过科学的数学模型和严格的质量控制，确保了系统的实用性和准确性。

---

*本分析基于 homtransform.py 中的完整实现，涵盖了校准系统的所有关键组件和数学原理。*