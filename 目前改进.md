
使用当前注视点序列 self.sliding_window_gaze_points = deque(maxlen=8)，计算其几何中心点作为当前凝视点坐标。实现以下逻辑流程进行跳转的目标点确定：

1. 中心点计算：对self.sliding_window_gaze_points中的所有坐标点进行算术平均，计算出精确的中心点坐标作为当前凝视点。

2. 初始UI控件检测：检查该中心点是否位于任何UI控件的矩形框范围内。
   a. 若检测到有效UI控件：将该UI矩形框的几何中心设定为目标点。
   b. 控件大小过滤：若检测到的UI控件矩形框宽度或高度大于350像素，则忽略该控件，执行默认传送操作。

3. 散布检测机制：若当前凝视点中心未检测到任何可控UI控件，启动散布检测：
   a. 以当前凝视点为圆心，100像素为半径的圆形区域内，随机均匀生成8个检测点。
   b. 对每个检测点执行UI控件检测，同样排除宽度或高度大于350像素的大型控件。
   c. 从所有有效检测结果中，选择距离原始凝视点最近的可控UI矩形框。
   d. 计算并传送到该UI矩形框的边界位置。

4. 默认传送机制：若上述所有检测均未发现有效UI控件，则将原始计算的凝视点中心点作为最终目标点进行传送。

确保所有坐标计算精确到像素级别，UI控件检测需考虑层级关系


